<div class="tab-pane fade" id="how_cite" role="tabpanel" aria-labelledby="how_cite-tab">
{% for key,reference in data.references %}
	<div class="card">
		<div class="card-header">
			<h5 class="mb-0">
				<button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapse{{ key }}" aria-expanded="true" aria-controls="collapse{{ key }}">
					{{ reference.style }}
				</button>
			</h5>
		</div>
		<div id="collapse{{ key }}" class="collapse" aria-labelledby="heading{{ key }}" data-parent="#how_cite">
			<div class="card-body">
				{{ reference.reference }} doi: <a href="http://doi.org/{{data.doi}}" target="_blank" rel="noopener noreferrer">{{ data.doi }}</a>
			</div>
		</div>
	</div>
{% endfor %}
</div>
